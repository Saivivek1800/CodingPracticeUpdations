# Frontend

UI templates for the automation dashboard live here.

- `templates/index.html`: one-click pipeline UI and live log viewer.

The Flask app is configured to load templates from `frontend/templates`.
