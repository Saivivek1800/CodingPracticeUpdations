# Virtual environment
venv/
.venv/
env/

# Session cookies (local only — expire; redundant if .secrets.env is in repo)
beta_admin_session.json
prod_admin_session.json
admin_session.json

# NOTE: .secrets.env and .secrets.enc are tracked so teammates can clone and run Phase 2.
# Use a PRIVATE repo only. If the repo is ever public or leaked, rotate all admin passwords immediately.

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Job logs and uploaded session payloads (can be large)
sessions/

# Environment files with real values
*.local.env
