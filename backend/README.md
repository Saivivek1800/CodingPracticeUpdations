# Backend

Backend automation lives in project root scripts and Python modules.

Main entrypoint:
- `app.py`: Flask API + queue worker.

Automation scripts:
- `run_full_pipeline.sh`
- `run_*_updater.sh`
- `auto_*_updater.py`

This `backend` folder is a documentation anchor for backend ownership and future code moves.
