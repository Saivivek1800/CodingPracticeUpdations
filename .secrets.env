# No plaintext USERNAME/PASSWORD here — use .secrets.enc + .secrets.key (see .secrets.env.example).
# If login fails, rebuild .secrets.enc from a temporary file with full KEY=value lines, then: bash setup_secrets.sh
BETA_DJANGO_ADMIN_URL=https://nkb-backend-ccbp-beta.earlywave.in/admin/
PROD_DJANGO_ADMIN_URL=https://nkb-backend-ccbp-prod-apis.ccbp.in/admin/
